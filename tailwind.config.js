/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{html,js,svelte,ts,astro}'],
  safelist: [
    'text-[0.85rem]',
    'text-[1.025rem]',
    // Add other arbitrary classes if needed
  ],
}